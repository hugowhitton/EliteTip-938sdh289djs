<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Staff Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Global CSS -->
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="/headers.css">
  <link rel="stylesheet" href="/footer.css">
</head>
<body>
  <header>
    <a href="/">EliteTip</a>
    <div class="profile-dropdown">
      <button class="profile-button">
        <img alt="Profile" class="profile-avatar" src="<%= user.profileImage ? user.profileImage : 'data:image/svg+xml;utf8,<!-- default svg -->' %>">
      </button>
      <div class="dropdown-content">
        <a href="/staff/edit">Edit Profile</a>
        <a href="/staff/logout">Logout</a>
      </div>
    </div>
  </header>
  <main>
    <h2>Welcome, <%= user.firstName %> <%= user.lastName %>! (Employee #<%= user.employeeNumber %>)</h2>
    <p>Email: <%= user.email %></p>
    <p>Role: <%= user.role %></p>
    <% if (user.role == 'maid') { %>
      <p><a href="/staff/tips">View Tips</a></p>
    <% } %>
    <% if (user.role === 'manager') { %>
      <p><a href="/manager/all-tips">Manager Panel</a></p>
      <p><a href="/manager/staff-list">View Database (Staff)</a></p>
    <% } %>
  </main>
  <footer>
    &copy; <%= new Date().getFullYear() %> EliteTip
  </footer>
</body>
</html>
